# Berth

**一个低内存、无插件负担的桌面工作台——管理文件夹、文件，以及并行运行的多个 AI 终端会话**

> 本版本聚焦 **macOS 单平台**，暂不考虑 Windows / Linux 兼容性。

---

## 0. 命名说明

**Berth**（船舶泊位）——产品的核心场景是:多个 AI Agent(claude code / codex / 其他 CLI harness)像一艘艘船，各自在不同的项目/worktree 上"停靠"工作，你在同一个码头里管理它们、随时切换查看。

---

## 1. 背景与问题定义

### 1.1 现状痛点

- 主流 IDE(VSCode 及其衍生)是"重"的：Electron 内核、Extension Host、大量常驻插件机制，空载内存普遍在 300–500MB+。
- 但当下的实际工作流已经变成：**真正的编码动作大量交给终端里的 AI Agent（claude code / codex 等）完成**，IDE 里的编辑器本体使用频率在下降，插件生态（调试器、Linter、补全）对这类工作流的价值也在下降。
- 市面上出现了一批"AI 终端会话管理器"（Claude Squad、Opcode、Nimbalyst、claude-sessions 等），验证了这个方向的真实需求，但要么是纯 CLI/TUI（无图形文件浏览），要么是完整重型 IDE 的自带功能（跟着母体一起重）。

### 1.2 产品假设

如果把"文件浏览查看"“多 AI 终端会话管理”这两件事做成一个**足够轻、只做这两件事**的独立工具，可以获得比现有方案更低的资源占用，同时不牺牲核心工作流的完整性。

### 1.3 平台范围

第一个版本**只做 macOS**（面向 MacBook 用户），不做跨平台适配。好处是：
- 不用管 Windows ConPTY 兼容性，pty 处理走标准 POSIX 接口即可
- "在访达中定位文件" 这类系统集成能力有统一、稳定的系统 API，不用做降级方案
- 终端/渲染层的开发和测试成本显著降低，可以把精力集中在核心体验打磨上

跨平台留作后续版本的规划项，不在本期需求范围内。

---

## 2. 目标用户与非目标

**目标用户**：日常主力工作流是"在终端里驱动 AI Agent 写代码，自己做 review/微调"的开发者，使用 MacBook 办公，对内存占用、启动速度敏感，不需要传统 IDE 的调试器/插件生态。

**非目标（明确不做）**：
- 不做插件系统 / 插件市场
- 不做完整代码编辑体验（多光标、LSP 智能补全、断点调试）——只做"能看、能改两笔"
- 不做自研 AI 对话 UI / 不自建会话历史存储——AI 交互完全通过内嵌终端跑现有 CLI 工具完成，历史记录直接复用这些工具自己的本地存储，不重复造轮子
- 本期不做 Windows / Linux 支持

---

## 3. 产品能力清单

| 模块 | 能力 |
|---|---|
| **文件管理** | 打开文件夹/文件、多文件夹并行打开（新窗口 / 追加到当前窗口的多根工作区）、文件夹树浏览、懒加载展开、拖拽移动、重命名、新建文件/文件夹、删除（回收站）、只读高亮查看、**可编辑并保存**、在访达中定位 |
| **Git** | **Git Diff 查看（只读对比，不做暂存/提交等写操作）** |
| **多媒体查看** | **Markdown 预览、PDF 查看、视频/音频播放** |
| **会话管理** | 历史会话索引（读取 claude code / codex 本地存储，挂在文件树对应文件夹下）、运行中会话的独立列表监控、一键恢复历史会话、多终端并行、可拖拽分屏/停靠布局、Git worktree 隔离（可选高级能力）、resize 联动、复制粘贴、scrollback、**拖拽文件到终端自动插入路径** |
| **布局** | **侧边栏（会话列表/文件树）可独立收缩**、**主工作区统一网格布局，任意面板可拆分、各自管理独立 Tab 页，支持跨面板拖拽移动 Tab** |
| **快捷短语** | 前缀触发的 prompt 片段库，输入时自动提示补全，一键展开注入终端（走 bracketed paste，避免多行内容被打断） |
| **窗口/工作区** | **窗口命名、多根工作区配置持久化、启动时按名称恢复上次打开的文件夹组合** |
| **设置** | 主题、字体、默认 shell、快捷短语的增删改查 |

---

## 4. 技术架构总览

```
┌──────────────────────────────────────────────┐
│              Tauri v2 应用外壳                  │
│      (系统 WebView 渲染，不打包 Chromium)         │
├──────────────────────────────────────────────┤
│ Rust 后端                                       │
│  ├─ pty 管理（POSIX，macOS 单平台简化）           │
│  ├─ SessionSource trait → 会话历史读取适配器      │
│  │    ├─ ClaudeCodeSource（解析 ~/.claude/projects/...jsonl，  │
│  │    │    session id = 文件名）                  │
│  │    ├─ CodexSource（解析 ~/.codex/sessions/...jsonl，       │
│  │    │    session id = 文件名）                  │
│  │    ├─ HermesSource（shell out `hermes sessions list/export`，│
│  │    │    不直接读其内部 SQLite，避免碰内部实现细节/WAL并发问题）│
│  │    └─ 增量 tail（notify 监听 + offset 续读，支持"实时查看"）│
│  ├─ std::fs / notify → 文件系统操作与变化监听      │
│  ├─ std::fs::write → 文件保存（编辑模式）          │
│  ├─ std::process → git diff（shell out 解析 unified diff）│
│  ├─ tauri asset协议 / convertFileSrc → 向WebView提供本地文件│
│  │    （PDF/视频/音频交给 WKWebView 原生能力渲染，不额外引入库）│
│  ├─ trash → 删除走回收站，不硬删                  │
│  ├─ rusqlite（轻量）→ 仅存 Berth 自身的运行时补充元数据 │
│  │    （进程存活状态、worktree/分支标签，不存对话内容）│
│  ├─ tauri-plugin-store → 应用设置 + 命名工作区持久化 │
│  └─ std::process → git worktree / 打开访达位置    │
├──────────────────────────────────────────────┤
│ 前端 (React + TypeScript + Vite)                │
│  ├─ zustand → 状态管理                           │
│  ├─ dockview → 多面板可拖拽布局                   │
│  ├─ xterm.js（MVP 终端渲染，见 4.2）              │
│  ├─ Shiki → 只读代码高亮（按需加载语言包）          │
│  ├─ CodeMirror6（懒加载）→ 编辑模式 + Diff 视图     │
│  ├─ react-markdown（懒加载）→ Markdown 预览        │
│  ├─ <embed>/<video>/<audio> 原生标签 → PDF/视频/音频│
│  └─ react-window / tanstack-virtual → 大文件虚拟滚动│
└──────────────────────────────────────────────┘
```

### 4.1 关键技术选型理由（简要）

| 选型 | 放弃的备选 | 理由 |
|---|---|---|
| Tauri | Electron | 系统自带 WebView，不打包 Chromium 内核，启动内存量级低一个数量级 |
| Shiki + 懒加载 CodeMirror6 | 常驻 Monaco/CodeMirror | 查看场景不需要完整编辑器状态机，编辑功能按需动态加载，绝大部分时间只有轻量高亮库常驻 |
| 直接解析 claude/codex 本地会话文件 | 自建会话数据库存对话历史 | 数据本来就在这些工具自己的 jsonl 里，重复存储既浪费空间也会产生数据不一致；Berth 只需读取，不需要拥有 |
| rusqlite（仅存运行时补充信息） | 完整会话数据库 | 对话内容、历史记录不归 Berth 管，本地库只存"这些工具本身没有的信息"（进程是否存活、worktree标签），数据量进一步缩小 |
| POSIX pty（不引入 Windows 分支代码） | portable-pty 全平台抽象 | 单平台场景下直接用标准 POSIX 接口更简单，少一层跨平台抽象的复杂度 |
| shell out `git diff`（解析 unified diff 文本） | `git2`(libgit2 绑定) | 只做只读对比展示，不需要暂存/提交/合并这些需要 libgit2 完整能力的写操作；直接调用系统 `git` 命令输出文本再解析，省掉引入 C 库绑定的体积和编译成本 |
| PDF/视频/音频交给 WKWebView 原生渲染 | pdf.js / video.js 等前端库 | macOS 上的 WKWebView 本身就能原生播放常见视频/音频格式、渲染 PDF（底层走系统的 AVFoundation / PDFKit），用 Tauri 的本地文件协议直接喂给 `<embed>`/`<video>`/`<audio>` 标签即可，不需要额外引入任何库，这也是选 WKWebView（而非自己实现渲染）在这几类文件上的额外红利 |
| tauri-plugin-store 存命名工作区 | 自建工作区数据库 | 工作区配置就是"名称 + 根目录路径列表 + 窗口布局"这种简单键值结构，不需要引入额外的存储方案 |
| **会话 ID 直接采用 claude code / codex / hermes 各自的原生 session id，Berth 不自造 ID** | 自建一套 Berth 内部会话 ID，与各工具 ID 做映射 | 历史会话（读文件/CLI输出）和运行中会话本来就是同一个东西的两种视图，用同一套 ID 贯穿，才能让"恢复历史会话 → 升格为运行中"这个动作不需要额外维护映射表；同时保证未来能直接用这个 id 调用工具自身的 `-r`/`resume`/`export` 等命令，不会出现"Berth 内部 ID 对不上工具自己的 ID"的情况 |

### 4.2 终端渲染方案：分阶段决策

三个候选方案对比过（调用系统 Terminal.app / alacritty_terminal 自绘 / xterm.js），结论是**不同阶段用不同方案**，不是一次性定死：

| 方案 | 内存表现 | 开发成本 | 适用性/成熟度 | 结论 |
|---|---|---|---|---|
| 调用系统 Terminal.app / iTerm2 | App 自身几乎零开销 | 低 | 差——多终端管理、快捷短语注入等核心功能无法实现，窗口跑在自己进程体系之外 | **不采用**，仅作为"在系统终端中打开"的兜底辅助入口保留 |
| alacritty_terminal + Canvas 自绘 | 更优，尤其多面板并行时优势会放大 | 高——渲染层、按键编码需自研，且 crate 本身非通用库设计，需自行踩坑 | 中——协议解析成熟（Zed 已验证），但暴露给使用方的边界情况需自行覆盖 | **作为路线图上的性能优化里程碑**，不放入 MVP |
| xterm.js（WKWebView 内） | 略高于 alacritty 方案，但在 16GB+ 内存的 MacBook 上多数情况下无感知 | 低——现成、成熟，VSCode/Hyper/JupyterLab 等广泛验证 | 高——冷门 CLI 工具的兼容性问题基本都被前人踩过 | **MVP 采用**，优先把核心产品价值（会话/文件管理）跑通验证 |

**决策依据**：内存差距在 macOS + 16GB 内存的主流配置下大概率不是用户能感知到的瓶颈，而"App 是否用 Electron"这一个决策本身已经带来了内存量级的下降。把工程资源优先投入验证"文件管理 + 会话管理"这个核心产品假设，比在 MVP 阶段死磕终端渲染层的极限性能更划算。`alacritty_terminal` 自绘渲染保留在第二阶段路线图中，作为明确的性能优化项。

### 4.3 终端通信架构（xterm.js 方案）

每个终端面板对应一条完整的双向管道，核心是三条独立的数据链路：

```
┌─────────────┐                    ┌──────────────┐                 ┌───────────┐
│  xterm.js    │  ① 用户按键/粘贴    │  Rust 后端     │  ③ 字节流写入    │   pty     │
│  (前端渲染)   │ ─────────────────→ │ (每个终端一个   │ ───────────────→│           │
│              │                    │  独立句柄)      │                 │  真实shell │
│              │  ② pty输出的字节流  │                │  ④ pty读到输出   │  进程      │
│              │ ←───────────────── │                │ ←───────────────│           │
└─────────────┘                    └──────────────┘                 └───────────┘
        ↕ resize事件双向同步（面板尺寸变化时）
```

**关键设计点**：这三条链路里，前后端都只做"字节搬运"，不做任何"这段字节是不是方向键、是不是回车确认"这类语义判断——这些交互逻辑完全在 claude code / codex 等 CLI 程序自己的进程内部处理（它们本身就是自己实现了 TUI 的程序），ANSI 转义序列的解析和渲染则完全交给 xterm.js。前后端唯一的职责是把字节可靠、低延迟地搬运到位。

**通信协议选型**：终端输出是高频小块数据（流式 AI 回复时可能每秒几十次推送），用 Tauri 的 **`Channel` API**，不用 `emit`/`listen` 广播事件——`Channel` 更接近一条按终端实例区分的专用流式管道，序列化开销更低，且天然按 `terminal_id` 隔离，不用自己在事件名里拼接区分。

**后端（Rust）核心结构：**

```rust
struct TerminalInstance {
    id: String,
    pty_writer: Box<dyn Write + Send>,   // 写入端，对应链路①③
    child: Box<dyn Child + Send>,        // 关闭会话时用于 kill
}
// 用 HashMap<String, TerminalInstance>（配合 Mutex）管理所有活着的终端实例

#[tauri::command]
async fn spawn_terminal(cwd: String, channel: Channel<Vec<u8>>) -> Result<String, String> {
    // 1. openpty，spawn 子进程（默认 shell 或指定命令）
    // 2. 后台线程持续读 pty 输出（对应链路②④），每读到一段就 channel.send(...)
    // 3. 读到 EOF（进程退出）时，额外发一个"已退出"信号，前端据此更新会话状态点
    // 4. 把 writer/child 存进 HashMap，返回 terminal_id
}

#[tauri::command]
fn write_to_terminal(terminal_id: String, data: Vec<u8>) -> Result<(), String> {
    // 对应链路①，前端 onData 拿到按键/粘贴字节，写进对应 pty
}

#[tauri::command]
fn resize_terminal(terminal_id: String, rows: u16, cols: u16) -> Result<(), String> {
    // 同时调用 pty resize（ioctl TIOCSWINSZ）通知子进程，和前端 fitAddon 联动
}

#[tauri::command]
fn kill_terminal(terminal_id: String) -> Result<(), String> {
    // 关闭会话时调用，避免孤儿进程；成功后是更新"运行中会话列表"状态为已退出的时机
}
```

**前端（React + xterm.js）核心结构：**

```ts
const term = new Terminal({ scrollback: 5000 });
const fitAddon = new FitAddon();
term.loadAddon(fitAddon);
term.open(containerEl);

const channel = new Channel<number[]>();
channel.onmessage = (data) => term.write(new Uint8Array(data)); // 链路②④，xterm.js自行解析ANSI并渲染
const terminalId = await invoke('spawn_terminal', { cwd, channel });

term.onData((data) => {
  invoke('write_to_terminal', { terminalId, data: Array.from(new TextEncoder().encode(data)) }); // 链路①
});

// 配合 ResizeObserver 监听面板容器尺寸变化
const handleResize = () => {
  fitAddon.fit();
  const { rows, cols } = term;
  invoke('resize_terminal', { terminalId, rows, cols });
};
```

**多终端隔离**：每个面板独立调用 `spawn_terminal`，拿到独立的 `terminal_id` 和独立的 `Channel`，互不干扰；后端的 `HashMap<terminal_id, TerminalInstance>` 是所有终端生命周期管理的唯一数据源。

**研发成本估算**：这套"IPC 通信 + pty 生命周期管理"是 xterm.js 和 alacritty_terminal 自绘两个方案**都跑不掉的公共工作量**，不存在"选 xterm.js 省了这块"的说法；xterm.js 方案省掉的是 ANSI 解析、渲染绘制、按键编码这三块（这是之前评估里"高成本"的主要来源）。量级参考：单终端闭环（含基础 resize、复制粘贴）预计 **3–5 人天**；扩展到多终端 dockview 管理 + 会话生命周期，再加 **3–5 人天**——跟文件树读写模块是同一数量级，不是项目里的最大风险点。

---

## 5. 交互设计

### 5.1 整体布局

```
┌──┬─────┬──────────────┬──────────────────────────┐
│⇤ │运行中│  文件树        │      主工作区（统一网格）    │
│  │会话  │  (含历史会话     │  任意面板可拆分/合并，       │
│  │(左侧 │   索引节点)     │  每个面板各自维护一组 Tab，  │
│  │ 栏)  │              │  Tab 可跨面板拖拽移动        │
│  │     │              │                          │
├──┴─────┴──────────────┴──────────────────────────┤
│ 底部状态栏：当前 cwd / 分支 / 快捷短语开关          │
└──────────────────────────────────────────────┘
```

- **侧边栏收缩**：会话列表、文件树各自可独立收起/展开（点击图标或把分隔线拖到底），收起后只留一条窄边（图标模式），点击图标临时展开或恢复常驻，收缩状态跟随工作区持久化保存
- **主工作区是单一的网格布局系统，不是"预览区"和"终端区"两个割裂区域**：任意面板都可以水平/垂直拆分成新的网格单元，每个网格单元自己维护一组 Tab（可以是文件查看/编辑 Tab，也可以是终端 Tab，或者混合承载），标签页可以从一个面板的 Tab 栏直接拖到另一个面板（或拖到面板边缘触发新的拆分）——这是 dockview 本身的原生能力，之前架构里选它正是为了覆盖这个需求，不需要额外造轮子

**两套会话相关的信息，用两种不同结构呈现**（这是本版本相较上一版的核心调整）：

| | 历史会话 | 运行中会话 |
|---|---|---|
| 数据来源 | 直接解析 claude code / codex 本地存储的 jsonl 文件，Berth 不重复存储 | Berth 自己维护的运行时状态（是否存活、worktree/分支标签） |
| 展示位置 | **挂在文件树对应文件夹节点下**，因为这些工具本身就是按项目路径索引会话的，和"这个路径下有什么"是同一性质的信息 | **独立的扁平列表**（左侧栏），因为需要跨路径、跨 worktree 持续监控"现在有几个 Agent 在跑" |
| 典型交互 | 展开文件夹节点看到"历史会话 (3)"，点进去看记录，点"恢复"重新接续 | 常驻可见，显示状态点（运行中/空闲/已退出），点击切换主工作区聚焦 |

两者通过**"恢复"动作衔接**：在文件树的历史会话节点里点"恢复"，本质是执行 `claude -r <session-id>` 之类的命令启动一个新进程，这个动作完成后，它会**升格**进入左侧运行中会话列表——不是两套割裂的系统，是"历史索引"通过一个动作转化为"实时监控对象"。

- 左侧栏：运行中会话列表，每项显示「项目名 + 分支/worktree 标签 + 进程存活状态点」，点击切换主工作区聚焦到对应终端
- 中间：文件树，支持多根（一个窗口内可并行浏览多个文件夹），每个根目录下多一个「历史会话」分组节点，展开显示该路径下 claude/codex 记录过的历史会话
- 主工作区：统一网格布局，用户可以自由拖拽拆分——比如左边一个终端 Tab、右边一个文件查看 Tab，或者两个终端并排对比不同 Agent 的输出，任意面板内可再新增/拖入更多 Tab

### 5.2 文件管理交互

- **打开文件夹**：首次打开（`文件 → 打开文件夹`）选中的文件夹会作为一个新窗口的工作区根目录；打开后 Berth 会自动检测该路径下是否有 claude/codex 历史会话记录，有则在文件树中显示「历史会话」分组
- **再打开一个文件夹时，二选一**（`文件 → 打开文件夹` 或拖拽文件夹进窗口都会触发这个选择）：

  | 方式 | 结果 |
  |---|---|
  | 在新窗口中打开 | 独立窗口，跟当前窗口互不影响，各自维护自己的运行中会话列表 |
  | 添加到当前窗口 | 追加为**同一棵文件树里的第二个根节点**，形成"多根工作区"（类比 VSCode 的 Multi-root Workspace），两个文件夹平级展示，各自独立展开/收起、各自有自己的「历史会话」分组 |

- **多根工作区的树形展示**：文件树顶层可以有多个根节点（文件夹A、文件夹B…），互不嵌套，每个根节点右键可以「从工作区移除」（只是从当前视图移除，不删除磁盘文件），也可以在根节点右键「在此处打开终端」「新建 AI 会话」，行为跟单文件夹时一致——多根工作区只是让你在一个窗口里同时浏览多个项目，不改变会话与目录绑定的规则（一个终端/会话仍然只绑定一个具体目录，不会跨根节点）
- **最近打开**：欢迎页/空状态展示最近打开过的文件夹列表，一键重新打开（同样支持"新窗口"/"追加到当前窗口"两种方式）
- **拖拽打开**：文件夹拖入窗口 → 触发上面的"新窗口 / 追加到当前窗口"选择；单个文件拖入窗口 → 在当前会话下直接以查看模式打开，不要求必须先打开文件夹
- **浏览**：点击文件夹展开/收起（懒加载，展开时才请求子目录内容）
- **拖拽移动**：树内节点直接拖拽到目标文件夹释放；跨磁盘移动自动 fallback 为"复制+删除"，过程中显示进度提示，不静默失败
- **重命名**：右键菜单 / 双击文件名进入行内编辑
- **新建**：右键菜单"新建文件/新建文件夹"，行内输入命名，回车确认
- **删除**：右键菜单删除 → 移入系统回收站，非永久删除，操作后有短暂的"撤销"提示条
- **查看文件**：单击文件在主工作区打开只读高亮视图；顶部有「编辑」按钮，点击后懒加载 CodeMirror6 切换为可编辑状态
- **编辑与保存**：进入编辑状态后，标签页标题旁出现一个圆点表示"未保存"；`Cmd+S` 或点击保存按钮写回磁盘（`std::fs::write`），保存后圆点消失；关闭有未保存改动的标签页会二次确认
- **外部改动冲突**：编辑过程中如果 `notify` 监听到该文件被外部进程（比如终端里的 AI Agent）修改了，弹出提示条"文件已在外部更改"，可选择「放弃本地改动、重新加载」或「忽略、保留当前编辑内容」，不做静默覆盖
- **Markdown 文件**：默认进入「预览」模式（渲染后的效果），顶部提供「预览 / 源码」切换，源码模式复用普通文本查看/编辑的逻辑
- **PDF / 视频 / 音频文件**：单击直接在主工作区打开对应的原生查看器（PDF 分页查看、视频/音频带播放控制条），这几类文件不支持编辑，只做查看/播放
- **在访达中定位**：右键菜单"在访达中显示"，调用 `open -R <path>` 定位并选中该文件（macOS 单平台，有统一系统 API，无需降级方案）
- **外部变更同步**：文件树通过 `notify` 监听目录变化，AI Agent 在终端里改了文件，树会自动刷新，无需手动操作

### 5.3 会话与终端管理交互

一共有三层不同粒度的"打开终端/会话"操作，交互上要分清：

| 操作 | 触发方式 | 粒度 | 说明 |
|---|---|---|---|
| 恢复历史会话 | 文件树「历史会话」节点下，点某条记录 | 历史 → 运行中 | 执行 `claude -r` / `codex resume` 等命令重新接续，成功后升格进入左侧运行中会话列表 |
| 在此处打开终端 | 文件树中任意文件夹右键 | 轻量、临时 | cwd 设为该文件夹，直接开一个终端面板，**不**进入左侧会话列表，不涉及 worktree，用完即关，类似 VSCode 的右键开终端 |
| 新建 AI 会话 | 左侧会话列表「+」按钮 / 打开文件夹 | 重量、持久 | 全新会话（非恢复），可选是否创建独立 git worktree 隔离，持续出现在左侧会话列表中 |
| 在系统终端中打开 | 终端面板标题栏菜单 | 兜底 | 唤起 Terminal.app / iTerm2（若已安装），逃生舱口，不作为主要使用路径 |

- **多终端布局**：终端面板遵循 dockview 的标准拖拽交互——拖动标签页可以左右/上下拆分，也可以拖出合并
- **关闭会话**：关闭面板时若对应进程仍在运行，弹出二次确认（防止误杀正在跑的 Agent 任务）；确认后 kill 掉子进程，进程结束后该会话从"运行中列表"移除，其历史记录仍可在文件树的历史会话节点里查到
- **resize**：面板拖拽调整大小时，实时联动通知子进程新的终端尺寸，界面即时重排（不会出现工具界面错位）
- **复制粘贴**：选中文本自动复制到系统剪贴板；粘贴时如检测到内容含多行，自动走 bracketed paste 包裹，避免被 Agent 的输入框拆成多次提交
- **拖拽文件到终端**：从文件树（或访达）把文件/文件夹拖到某个终端面板上方，拖拽过程中面板上显示一个提示标签（tag），松手后把该文件的绝对路径以 shell 安全转义的形式（含空格/特殊字符时自动加引号）作为文本插入到当前输入行光标处，不自动回车提交；多选文件拖入时，路径之间以空格分隔依次插入

### 5.4 快捷短语交互

- 每个终端面板旁有一个独立的输入框（不是终端本身），支持：
  - 输入 `/` 或自定义前缀触发下拉候选列表
  - 方向键选择、回车/Tab 确认注入
  - 注入内容以"整段一次性写入 pty”的方式发送，走 bracketed paste 保护
- 设置页单独管理快捷短语库：新增/编辑/删除，字段为「前缀」「展开内容」「分类」，支持按分类筛选

### 5.5 设置交互

- 简单表单式设置页：主题（跟随系统/浅色/深色）、终端字体与字号、默认 shell、默认工作区根目录
- 快捷短语管理入口在设置页内，同一页面完成

### 5.6 Git Diff 交互（只读对比）

- **定位改动文件**：文件树中，相对 Git 有改动的文件/文件夹显示状态标记（新增/修改/删除），跟大多数 Git 工具的树形展示一致
- **打开 Diff**：单击有改动标记的文件，或右键选择"查看 Diff"，在主工作区以 Diff 视图打开（默认对比工作区当前内容 vs HEAD），跟"查看/编辑"是同一个文件标签页的不同模式，顶部可切换「查看 / 编辑 / Diff」
- **展示形式**：并排双栏或行内 unified diff（MVP 先做行内 unified diff，实现和渲染成本更低），新增行/删除行用背景色区分，不支持在 Diff 视图里直接编辑内容——**这是只读对比，不做暂存(stage)、提交(commit)、丢弃改动(discard)这些写操作**，真正要做这些事，交给终端里的 git 命令或 AI Agent 完成，符合"文件管理只做查看+基础编辑，重活交给终端"的产品定位
- **多根工作区下的 Diff**：每个根节点下的改动状态独立计算（各自对应各自仓库的 Git 状态），互不影响

### 5.7 窗口与工作区管理

- **命名窗口**：一个窗口打开的文件夹组合（单个或多根）可以被命名保存为一个"工作区"。入口：窗口标题栏点击当前标题（默认显示为文件夹名，多根时显示"文件夹A, 文件夹B"），弹出输入框重命名，比如改成"AB"
- **持久化内容**：工作区保存的是「名称 + 根目录路径列表」，不包含运行中的终端进程状态（进程本身不能跨重启持久化，重启后需要的话可以从文件树的历史会话节点手动"恢复"）
- **启动时恢复**：应用启动的欢迎页，除了之前的"最近打开的文件夹"列表，新增"已保存的工作区"列表（显示命名 + 包含的根目录数量），点击直接按保存时的多根配置一次性打开对应窗口，不用重新一个个添加文件夹
- **未命名工作区**：没有主动命名的窗口，仍然按"最近打开"逻辑处理（用路径拼接自动生成一个默认展示名），命名是可选的便利功能，不是强制流程
- **路径失效处理**：恢复工作区时如果某个根目录已被移动/删除，该根节点在文件树里显示"路径不可用"的占位状态，不影响工作区里其他仍然有效的根目录正常打开

---

## 6. 非功能需求

| 指标 | 目标值 | 说明 |
|---|---|---|
| 应用壳常驻内存（不含终端内运行的 Agent 进程本身） | ≤ 100MB | 空载 + 若干终端面板打开但无高频输出时，基于 xterm.js 方案 |
| 冷启动时间 | ≤ 1s | 系统 WebView 复用，无需拉起独立 Chromium |
| 打包体积 | ≤ 15MB | 得益于不内置浏览器内核 |
| 历史会话列表加载 | 展开文件夹节点后 ≤ 100ms 出现 | 只读文件元数据/末尾预览，不整份解析 jsonl（见架构讨论中的"只 seek 文件末尾拿摘要"策略） |
| 大文件查看（100MB+ 日志/文本） | 流畅滚动，不整体读入内存 | 依赖分块读取 + 虚拟滚动 |
| 高频终端输出（流式 AI 回复）渲染 | 无明显卡顿 | xterm.js 自身滚动区优化 + 合理的 scrollback 上限设置 |

---

## 7. MVP 范围与迭代路线

**第一阶段（MVP，建议顺序）**
1. 打开文件夹 / 文件树浏览 + 只读查看（建立基本可用感，风险最低）
2. `SessionSource` 适配器：解析 claude code 本地会话文件，文件树下展示历史会话分组（先支持一家，验证可行性再扩展 codex）
3. 终端核心闭环：xterm.js + pty，单终端可用，resize、复制粘贴正常
4. 多终端 dockview 布局 + 运行中会话列表管理，打通「恢复历史会话 → 升格进运行中列表」的完整链路
5. 文件可编辑 + 保存（CodeMirror6 懒加载 + `Cmd+S` + 外部改动冲突提示）
6. 多媒体文件查看（Markdown 预览、PDF、视频、音频）
7. 快捷短语基础版（输入框 + 下拉 + 注入）
8. 命名工作区：保存/恢复多根文件夹组合
9. 设置页

**第二阶段**
- Codex / 其他 harness 的 `SessionSource` 适配器补齐
- Git worktree 集成（创建/清理/状态提示）
- **Git Diff 查看**（文件树改动标记 + unified diff 视图）
- **终端渲染层迁移评估**：实测 xterm.js 在多终端并行 + 高频流式输出场景下的内存/性能表现，决定是否启动 `alacritty_terminal` + Canvas 自绘渲染的迁移
- 大文件分块读取优化

**暂不做（明确延后）**
- Git 写操作（暂存/提交/丢弃改动/合并冲突解决）——只做只读 Diff
- 鼠标上报模式、图片协议（Sixel/Kitty graphics）、连字
- 任何形式的插件机制
- Windows / Linux 支持

---

## 8. 风险与开放问题

- claude code / codex 的本地会话存储格式属于**未公开承诺稳定性的内部实现**，可能随工具版本升级而调整字段/路径，`SessionSource` 适配器需要做好版本探测和优雅降级（解析失败时至少不影响文件树本身的正常使用）
- 同一仓库的多个 worktree 会被 claude/codex 索引成完全不相关的路径，文件树下"历史会话"节点天然是按路径割裂展示的，无法在文件树层面呈现"这几个 worktree 属于同一仓库"这层关系，这层信息仍需运行中会话列表侧的 worktree 标签来补充
- xterm.js 方案下，多终端并行且高频输出场景的内存曲线需要实测建立基线，作为后续是否迁移到 `alacritty_terminal` 自绘渲染的决策依据
- 若后续启动 `alacritty_terminal` 迁移：该 crate 并非为通用库使用设计，API 随版本演进可能有 breaking change，需锁定版本、预留适配成本
- 键盘输入到转义序列的编码逻辑（无论用 xterm.js 还是自绘方案）都需要覆盖 Ctrl/Alt 组合键等边界情况，上线前建议用真实的 claude code / codex 交互场景做完整走查
- 三层"打开终端/会话"入口（恢复历史 / 临时终端 / 新建会话）需要在 UI 上做出清晰的视觉区分，避免用户混淆各自的持久化语义
- 多根工作区场景下，每个根节点都要独立起一个 `notify` 监听实例，根节点数量较多时需要关注文件监听的句柄/资源开销，不能无限制叠加
- `git diff` 走 shell out + 文本解析，比起 libgit2 结构化调用更依赖"用户机器上 git 版本的输出格式稳定"，需要锁定解析逻辑基于标准 unified diff 格式，对极端情况（二进制文件 diff、超大 diff）要有专门的降级展示（比如提示"文件过大/为二进制，不展示 diff"，而不是尝试渲染卡死）
- 编辑保存与外部改动冲突的提示逻辑，在"AI Agent 高频写文件"的场景下可能被频繁触发（比如用户刚打开编辑，Agent 那头几乎同时又改了这个文件），需要设计合理的提示节流，避免用户被打断个不停
- WKWebView 对 PDF/视频/音频的原生渲染能力覆盖常见格式，但比较冷门的编码格式（比如某些小众视频编码）可能无法直接播放，MVP 阶段先支持系统层面能原生播放的格式，不做额外的转码/兼容层
- 命名工作区恢复时的路径失效处理（根目录被移动/删除）需要覆盖测试，避免因为一个失效路径导致整个工作区打开流程报错中断
- `Channel` 高频推送场景下（比如 AI 流式输出、`cat` 大文件），需要关注单次 `send` 的数据块大小和调用频率的平衡——块太小会导致调用次数过多、IPC 开销上升；块太大会导致渲染出现明显的"一次性刷一大段"卡顿感，建议按固定时间窗口（几毫秒到十几毫秒）攒批后再推送，而不是 pty 读到多少就立刻推多少
- 拖拽文件插入路径时的 shell 转义规则需要覆盖常见边界情况（路径含空格、引号、中文、emoji），转义逻辑建议统一在 Rust 侧维护一份，不要前后端各写一份不一致的转义实现
- 侧边栏收缩状态、面板网格布局跟随"工作区"一起持久化，意味着保存的工作区配置这个数据结构需要包含布局信息，不只是根目录路径列表，这块在实现命名工作区（5.7节）时要一并设计好数据结构，避免后续返工